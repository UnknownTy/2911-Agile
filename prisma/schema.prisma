datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}


model Country {
  id        Int      @id 
  name      String   @unique
  updatedAt DateTime @default(now())
  createdAt DateTime  
  stats     Json?
}
